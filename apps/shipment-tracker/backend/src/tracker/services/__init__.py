"""Business logic services -- canonical owners of domain logic.

Each service module is stateless: functions accept a database connection
and return data. No other module should duplicate queries defined here.
"""
